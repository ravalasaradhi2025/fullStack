 clone Repo
 do pip install -r requirements.txt
  open http://localhost:5000/ from browser to submit user form

from EC2

sudo yum update -y
sudo yum install python3 git -y

git clone https://github.com/your-repo.git
cd your-repo
python app.py
http://<EC2-Public-IP>:5000/


from docker
docker build -t my-fullstack-app .
docker run -p 5000:5000 my-fullstack-app

